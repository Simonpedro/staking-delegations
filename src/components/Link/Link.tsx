import { styled } from "stitches.config";

const Link = styled("a", {
  backgroundColor: "inherit",
  border: "0",
  color: "$mainBlue",
  cursor: "pointer",
  outline: "0",
  textDecoration: "underline",
  margin: "auto 0"
});

export default Link;
